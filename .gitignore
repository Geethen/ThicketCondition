# Local authentication and notebooks
gee_auth.ipynb
.ipynb_checkpoints/

# Runtime logs and generated checkpoints
*.log
analysis/outputs/

# Python caches and virtual environments
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local environment files
.env
.env.*

# Earth Engine service-account key — NEVER commit; used only locally / as a CI secret
*sa*.json
gee_service_account*.json
ee-service-account*.json
# ...but `*sa*.json` also swallows the sampling design records, which are the
# written record of how each round's sample was allocated and must be tracked.
!analysis/results/sample_design_*.json

# OS/editor noise
Thumbs.db
.DS_Store

# Large exported rasters (do not commit)
*.tif

# Large local input geodatabases (>100 MB; exceeds GitHub's file limit)
# BioregionsSimple.gdb.zip is read by analysis/26_augment_vegtype_2022strat.py
*.gdb.zip

# Inspector dev-only artifacts (index.html is the built, committed output)
inspector/node_modules/
inspector/package-lock.json
inspector/preview.png
inspector/shot.mjs
# Baked EE tile manifest holds temporary, account-tied tokens — regenerated in CI
inspector/gee_layers.json
inspector/assignment_links.csv

# Local cloud-native geospatial experiments and benchmark artifacts
CNGexploration/
