# Local authentication and notebooks
gee_auth.ipynb
.ipynb_checkpoints/

# Runtime logs and generated checkpoints
*.log
analysis/outputs/

# Python caches and virtual environments
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Local environment files
.env
.env.*

# Earth Engine service-account key — NEVER commit; used only locally / as a CI secret
*sa*.json
gee_service_account*.json
ee-service-account*.json

# OS/editor noise
Thumbs.db
.DS_Store

# Large exported rasters (do not commit)
*.tif

# Inspector dev-only artifacts (index.html is the built, committed output)
inspector/node_modules/
inspector/package-lock.json
inspector/preview.png
inspector/shot.mjs
# Baked EE tile manifest holds temporary, account-tied tokens — regenerated in CI
inspector/gee_layers.json